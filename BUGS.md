# Bug 记录

## BUG-010 · 点击其他区域，设置/记忆面板不会关闭

- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/components/Sidebar.vue`、`src/App.vue`

### 现象
打开设置面板或记忆面板后，点击聊天区等其他位置，面板依然开着，需要再点一次开关才能关。

### 根因
面板没有"点击外部关闭"的逻辑。

### 修复
- 面板容器加 ref，挂全局 `click` 监听：点击位置不在容器内 → 关闭。
- `onBeforeUnmount` 时移除监听，避免泄漏。
- 设置面板（Sidebar）与记忆面板（App）都加了。

### 经验教训
- 弹层/浮窗的标准交互是"点击外部关闭"，用 `document.addEventListener('click')` + `contains` 判断，记得卸载时移除。


- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/i18n.js`（新增）、`App.vue`、`Sidebar.vue`、`ChatInput.vue`、`MessageList.vue`

### 现象
设置面板里切到 English，只有 AI 的**应答语言**变了，页面按钮/文案仍是中文。

### 根因
语言设置最初只改了系统提示词的应答语言指令，**没有做界面文案的国际化（i18n）**。

### 修复方案
新建 `src/i18n.js`（共享 `lang` + `t()` 字典），侧边栏/顶栏/欢迎页/输入框/推荐问题等所有文案按语言切换。

### 经验教训
"切换语言"对用户意味着**整个界面**都变语言，只改模型回复不算完成。


- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/components/Sidebar.vue`

### 现象
点击侧边栏底部的 ⚙️ 齿轮，设置面板始终不出现。第一次以为是层叠问题（加了 z-index 仍无效）。

### 根因
设置面板 `.settings-panel` 是 `.side-footer` 的**兄弟节点**，其 `position: absolute; bottom: calc(100% + 4px)` 中的 `100%` 解析为**包含块**（最近的定位祖先，即整个侧边栏）的高度——面板被定位到屏幕上方之外，等于"渲染在视口外面"，所以看不见。

### 修复
把设置面板**移进 `.side-footer` 内部**，使其包含块变为页脚本身；`bottom: calc(100% + 4px)` 即"页脚上方 4px"，面板正常向上弹出。
同时给 `.sidebar` 加 `position: relative; z-index: 10` 建立层叠上下文，确保浮层压过主区。

### 经验教训
- `bottom: 100%` / `top: 100%` 相对的是**包含块**（最近的定位祖先），不是视觉上的"父容器"。
- 想让浮层相对某个元素定位，就把它放进该元素内部（该元素设 `position: relative`），再偏移。

---

## BUG-007 · "正在联网搜索"文字竖着排列

- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/components/MessageItem.vue`

### 现象
联网搜索时 AI 消息区显示"正在联网搜索"，文字**竖着排**，不美观。

### 根因
CSS 选择器冲突：`.thinking span` 给所有 span 设了 `width: 7px; height: 7px; border-radius: 50%`（小圆点样式），
而"正在联网搜索"文字也是个 span，被塞进 **7px 宽的盒子**里，中文字符只能逐字换行 → 竖排。

### 修复
- 三个小圆点改用独立类 `.dot`，样式只作用于圆点，不再波及文字。
- `.thinking` 改为块级 flex + `white-space: nowrap`，文字永不换行。

### 经验教训
- 泛化后代选择器（`.x span`）容易误伤同结构里的其他元素；给不同角色用独立类名。

---

## BUG-006 · 没点联网开关，模型却回答"已联网"

- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/App.vue`（系统提示词）

### 现象
开关处于关闭状态（后端日志 `webSearch=false` 可证），问模型"是否联网"却回答"已经联网"。

### 根因
不是开关 bug，是**提示词表述歧义**：上一版提示词写"你具备联网搜索能力"，7B 模型把"具备联网能力"误解为"当前已联网"，被问"你联网了吗"就顺口答"是的"。

### 修复
最终方案（最稳）：**联网状态不由模型判断，由后端按开关直接注入**。
- `server/chat.js` 每次请求都注入一条 system 声明：
  - 开关开 → "你当前已联网（用户开启了联网搜索）。若被问到'是否联网'，直接回答'已联网'。"
  - 开关关 → "你当前未联网（用户未开启联网搜索）。若被问到'是否联网'，直接回答'未联网'。"
- 系统提示词里删掉让模型"自己判断联网"的段落（那是 BUG-006 的诱因）。

### 验证
两来回测试（开→关→开→关）全部跟随开关：关=未联网 / 开=已联网 ✅

### 经验教训
- 让 LLM 自我声明某个状态（联网/权限/角色）不可靠，会随机波动。
- 权威状态应由后端/系统注入为显式事实，模型只负责复述。
- 给 LLM 声明"具备某能力"，会被它当成"当前就有该状态"。要区分"能力"与"当前状态"。

---

## BUG-005 · 开启联网后回答仍用"可能/大概"推测

- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`server/chat.js`（搜索上下文）、`src/App.vue`（系统提示词）

### 现象
开启联网搜索后提问，回答仍充满"可能是…""大概…"的推测，用户觉得没有真正联网。

### 根因
搜索确实命中并注入了资料（后端日志可证），但 7B 模型指令跟随弱，注入的搜索上下文措辞较软（"请结合资料回答"），模型遇到不确定就退回自己的知识去"推测"。

### 修复
1. 搜索上下文指令加强：**必须直接依据资料给明确、确定的答案，禁止"可能/大概/也许"**；资料没有相关信息时明确说"未找到直接答案"。
2. 系统提示词同步加强，联网时禁止推测性措辞。

### 经验教训
- 给模型注入资料时，指令要"硬"（禁止什么、必须什么），不能只说"请参考"。
- 小模型（7B）更需要明确约束，否则倾向保守推测。

---

## BUG-004 · 背景随对话上下滚动而变化

- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/components/MessageList.vue`

### 现象
logo 背景会随着消息列表上下滚动而移动，不固定。

### 根因
背景层 `.msg-bg` 是滚动容器 `.msg-list` 的子元素，`position: absolute` 的绝对定位元素会随滚动容器的内容一起滚动。

### 修复
把背景层移到滚动容器的**外层**：新增 `.msg-wrap`（`position: relative; overflow: hidden`）作为背景与滚动的公共父层。
- `.msg-bg`：absolute 铺满 `.msg-wrap`，固定不动。
- `.msg-list`：absolute 铺满并自己滚动，背景从底下透上来。

### 经验教训
- "背景固定、内容滚动"的标准做法：把背景放在滚动容器的兄弟层（或父层），而不是子层。
- 绝对定位元素在滚动容器内会跟着内容滚，别拿它当固定背景。

---

## BUG-003 · 开启联网后，问模型"是否联网"答"未联网"

- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/App.vue`（系统提示词）、`src/components/MessageItem.vue`

### 现象
开启「联网搜索」后提问，感觉回答"没有联网"；直接问 AI「你联网了吗」它回答"未联网"。

### 排查
后端日志显示 `webSearch=true` 收到、搜索命中 N 条结果并注入——**功能实际正常**。对比测试：同一个需实时信息的问题，
不开联网回答靠猜测（"2026年可能举办世界杯"），开启后明确引用检索事实（"2026被定为可持续发展志愿者国际年"）。
结论：**问题在模型不知道自己能联网**。LLM 本身无网，系统提示词又没告诉它联网资料会注入，于是被问"是否联网"时老实答"未联网"。

### 修复
1. 系统提示词告知模型：收到"联网搜索到的资料"即代表已联网，请优先基于资料回答并如实说明；否则说明未联网。
2. AI 消息等待区：联网开启时显示「🔍 正在联网搜索…」动画，让搜索过程可见（而非只有小圆点）。

### 经验教训
- 给 LLM 加工具/能力后，必须在提示词里让它"知道自己有这能力"，否则它会按默认认知回答。
- 功能是否生效先看后端收到的参数与日志，别被模型回答误导。

---

## BUG-002 · 未开启联网开关，回复却走了联网搜索

- **日期**：2026-08-19
- **状态**：🔍 已排查，属"开关状态不显眼"导致的误判，已加醒目标识
- **文件**：`src/components/ChatInput.vue`、`src/App.vue`、`server/chat.js`

### 现象
用户说没有点击「联网搜索」开关，但 AI 回复却像搜了网（慢、带"根据我检索到的信息"这类话）。

### 排查
1. 代码接线核查：`App.vue` 里 `webSearch = ref(false)` 默认关闭；`handleSend` 传 `webSearch.value`；`ChatInput` 用 `defineModel('webSearch')` 双向绑定。**没有任何不点击就把开关置 true 的代码路径**。
2. 后端日志：出现多次 `[webSearch] 检索到 N 条结果`，说明确实有请求带着 `webSearch=true` 进来——即当时浏览器会话里开关是开着的。
3. 结论：开关被开启（很可能之前试功能时点开的），但开启状态只有小圆点变紫，**视觉提示太弱**，用户没察觉到。

### 修复
- 顶栏加「🌐 联网中」高亮徽标，开启即一目了然。
- 后端 `chat.js` 每条请求打日志 `[chat] model=.. webSearch=.. messages=..`，之后可直接定位。
- 开关本身是内存态，**刷新页面即恢复关闭**。

### 经验教训
- 影响用户预期/行为的开关，状态必须有"一眼可见"的提示，不能只靠小图标。
- 排查此类问题先看后端收到的真实参数（日志），不要凭现象猜。

---

## BUG-001 · 流式回答不显示，切换会话后才出现

- **日期**：2026-08-19
- **状态**：✅ 已修复
- **文件**：`src/App.vue`
- **严重度**：高（核心功能失效）

### 现象
在对话里提问后，AI 一直显示"思考中"动画，回答不出现；**切换到别的会话再切回来**，回答才完整显示。

### 根因
Vue 3 响应式陷阱。

`handleSend` 里先创建了一个**普通对象**作为占位回复：
```js
const reply = { role: 'assistant', content: '' }
session.messages.push(reply)   // push 时 Vue 把它包了一层 reactive 代理
```
`session.messages` 是响应式数组，push 时 Vue 会把这个对象包进 `reactive()` 代理。但 JS 变量 `reply` 仍然指向**原始对象**。

流式回调里：
```js
onContent: (chunk) => { reply.content += chunk }
```
改的是**原始对象**，绕过了 reactive 代理，不会触发 Vue 的依赖更新，所以视图不刷新。只有等切换会话这类操作引发重新渲染时，Vue 重新读值才看到已累积的完整内容——表现为"切换后突然出现"。

### 修复
用 `reactive()` 创建占位对象，让赋值走响应式代理：
```js
import { reactive } from 'vue'
const reply = reactive({ role: 'assistant', content: '' })
session.messages.push(reply)
```

### 经验教训
- 先创建对象、后 push 进响应式数组，再在外部持续修改该对象 → **必须用 `reactive()`**。
- 这也是为什么 Vue 推荐"不要在 setup 里持有原始对象引用去改值"。
